version: '3.7'

services:
    nginx:
        build:
            context: .
            dockerfile: Dockerfile_nginx
        image: /nginx:latest
        ports: 
            - "80:80"
        container_name: nginx_springproject
        network_mode: "host"